<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>P-Arts Passport Login</title>
  <!-- Enhanced font loading with fallbacks -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Crimson+Text:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-gradient: linear-gradient(135deg, #fae8ea 0%, #f5f0f6 50%, #e5f5ee 100%);
      --primary: #edccd2;
      --soft-purple: #f5f0f6;
      --soft-green: #e5f5ee;
      --leaf-green: #a6c8b5;
      --brand-deep: #463b38;
      --headline: #22212a;
      --text: #4a4a56;
      --text-light: #6b6b78;
      --cta-pink: #efb3c0;
      --cta-pink-hover: #f8d4e3;
      --border-card: #ebd2da;
      --white: #ffffff;
      --shadow-light: 0 4px 20px rgba(234, 210, 212, 0.15);
      --shadow-medium: 0 8px 32px rgba(234, 210, 212, 0.2);
      --shadow-heavy: 0 12px 48px rgba(234, 210, 212, 0.25);
    }

    @font-face {
        font-family: 'digitfont';
        src: url('./digifont.ttf') format('truetype');
    }

    * { 
      box-sizing: border-box; 
      margin: 0;
      padding: 0;
    }
    html { 
      scroll-behavior: smooth; 
      background: var(--bg-gradient);
      min-height: 100vh;
    }
    body {
      background: var(--bg-gradient);
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      color: var(--text);
      line-height: 1.6;
      font-size: 16px;
      overflow-x: hidden;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 24px;
    }
    .back {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--leaf-green);
      color: var(--brand-deep);
      padding: 12px 24px;
      border-radius: 25px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      box-shadow: var(--shadow-light);
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      margin-bottom: 24px;
      font-family: 'Inter', sans-serif;
    }
    
    .back:hover {
      background: var(--cta-pink);
      transform: translateY(-2px);
      box-shadow: var(--shadow-medium);
    }

    .passport-login {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 24px;
      padding: 48px 40px;
      margin: 0 auto;
      box-shadow: var(--shadow-heavy);
      border: 1px solid rgba(235, 210, 218, 0.3);
      width: 100%;
      max-width: 450px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .passport-login::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--cta-pink), var(--leaf-green));
    }

    .introt {
      font-family: 'Crimson Text', serif;
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--headline);
      margin-bottom: 24px;
      line-height: 1.3;
    }

    .login-form {
      display: flex;
      flex-direction: column;
      gap: 24px;
      margin-top: 32px;
    }

    .form-group {
      width: 100%;
      display: flex;
      flex-direction: column;
    }

    .form-input {
      width: 100%;
      padding: 16px 20px;
      border-radius: 12px;
      border: 2px solid var(--border-card);
      background: rgba(255, 255, 255, 0.8);
      color: var(--text);
      font-size: 1rem;
      font-family: 'Inter', sans-serif;
      transition: all 0.3s ease;
      font-weight: 500;
    }

    .form-input:focus {
      outline: none;
      border-color: var(--leaf-green);
      background: var(--white);
      box-shadow: 0 0 0 3px rgba(166, 200, 181, 0.2);
    }

    .form-input::placeholder {
      color: var(--text-light);
      font-weight: 400;
    }

    .login-button {
      width: 100%;
      padding: 16px 24px;
      border-radius: 12px;
      border: none;
      background: var(--cta-pink);
      color: var(--brand-deep);
      font-size: 1.1rem;
      font-weight: 600;
      font-family: 'Inter', sans-serif;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-top: 16px;
      box-shadow: var(--shadow-medium);
    }

    .login-button:hover {
      background: var(--leaf-green);
      transform: translateY(-2px);
      box-shadow: var(--shadow-heavy);
    }

    .logo-img {
      width: 80px;
      height: 80px;
      margin: 0 auto 24px;
      border-radius: 50%;
      box-shadow: var(--shadow-light);
    }

    @media (max-width: 480px) {
      body {
        padding: 16px;
      }
      
      .passport-login {
        padding: 32px 24px;
        border-radius: 16px;
      }
      
      .introt {
        font-size: 1.3rem;
      }
      
      .form-input,
      .login-button {
        padding: 14px 16px;
        font-size: 16px; /* Prevents zoom on iOS */
      }
      
      .back {
        padding: 10px 20px;
        font-size: 0.9rem;
      }
    }
  </style>
</head>
<body>
  <div class="back" onclick="javascript:history.back()">← BACK</div>
  <div class="passport-login">
    <div class="introt">Passport<br>Project Arts & Affirmations</div>
    <img src="resources/logo.png" alt="P-Arts Logo" class="logo-img">
     
    <form action="/login" method="POST" class="login-form">
      <div class="form-group">
        <input type="text" name="username" placeholder="Username" required class="form-input" autocomplete="username">
      </div>
      <div class="form-group">
        <input type="password" name="password" placeholder="Password" required class="form-input" autocomplete="current-password">
      </div>
      <button type="submit" class="login-button">Login</button>
    </form>
  </div>
</body>
</html>
